import MainWrapper from "../components/MainWrapper";

interface Props {}

const Home = (props: Props) => {
  return (
    <MainWrapper>
      <div className="page__headline">
        <h1>Home</h1>
      </div>
    </MainWrapper>
  );
};

export default Home;
