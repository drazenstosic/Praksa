import MainWrapper from "../components/MainWrapper";

interface Props {}

const Home = (props: Props) => {
  return (
    <MainWrapper>
      <div>
        <h1>Home</h1>
      </div>
    </MainWrapper>
  );
};

export default Home;
